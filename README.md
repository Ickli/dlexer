Collection of parsers and parser generators (basic regex and others).

Regex supports:
    Unicode units;
    Capturing groups ();
    Repeating symbols or groups */+/? ;
    Lazy operators;
    Ranges

## Note:
Parser generation is implemented only for regex
